import { View, Text } from "react-native";
import React from "react";

const SwitchLanguage = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Switching language</Text>
    </View>
  );
};

export default SwitchLanguage;
